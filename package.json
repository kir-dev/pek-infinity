{
  "name": "@workspace/root",
  "private": true,
  "workspaces": [
    "full-stack"
  ],
  "scripts": {
    "generate": "npx prisma generate --no-hints --require-models"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.0",
    "@prisma/client": "^7.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "dotenv": "^17.3.1",
    "prisma": "^7.4.0"
  }
}
