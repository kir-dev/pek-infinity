{
  "scripts": {
    "generate:prisma": "prisma generate --require-models --no-hints",
    "postinstall": "npm run generate:prisma"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "prisma": "^6.17.1"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1"
  }
}
