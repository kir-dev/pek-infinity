{
  "name": "@workspace/root",
  "private": true,
  "workspaces": [
    "full-stack"
  ],
  "scripts": {
    "generate": "npx prisma generate --no-hints --require-models"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.1.0",
    "@prisma/client": "^7.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "dotenv": "^17.2.3",
    "prisma": "^7.1.0"
  }
}
